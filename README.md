
  # Design Career Path v0.1 (Copy)

  This is a code bundle for Design Career Path v0.1 (Copy). The original project is available at https://www.figma.com/design/4Tms7uACEcuh6kss1CQqbo/Design-Career-Path-v0.1--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  